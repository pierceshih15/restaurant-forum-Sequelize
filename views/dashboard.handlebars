<div class="row">
  <div class="col-md-12">
    <h1>{{restaurant.name}}</h1>
    <p>[{{restaurant.Category.name}}]</p>
  </div>

  <hr>
  <div class="container">
    <ul>
      <li>評論數：{{restaurant.Comments.length}}</li>
      <li>瀏覽次數：{{restaurant.viewCounts}}</li>
      <li>收藏數：</li>
    </ul>

    <strong><a class="" href="/restaurants/{{restaurant.id}}">
        回上一頁
      </a></strong>
  </div>

</div>